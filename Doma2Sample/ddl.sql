create table employee (
    id int,
    name varchar(100),
    email varchar(100),
    job_type int,
    primary key(id)
);

create table company_section (
    id int,
    name varchar(100),
    primary key(id)
);